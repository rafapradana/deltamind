import 'package:flutter/material.dart';

/// App color constants
class AppColors {
  /// Primary Blue
  static const Color primary = Color(0xFF0056D2);

  /// Light Blue (Secondary)
  static const Color secondary = Color(0xFF33A1FD);

  /// Accent color (using Light Blue for accent highlights)
  static const Color accent = Color(0xFF33A1FD);

  /// Black
  static const Color black = Color(0xFF000000);

  /// White
  static const Color white = Color(0xFFFFFFFF);

  /// Gray
  static const Color gray = Color(0xFF4A4A4A);

  /// Background color
  static const Color background = Color(0xFFFFFFFF);

  /// Surface color
  static const Color surface = Colors.white;

  /// Error color
  static const Color error = Color(0xFFB00020);

  /// Success color
  static const Color success = Color(0xFF4CAF50);

  /// Warning color
  static const Color warning = Color(0xFFFFC107);

  /// Info color (using primary blue)
  static const Color info = Color(0xFF0056D2);

  /// Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF4A4A4A);
  static const Color textDisabled = Color(0xFFBDBDBD);

  /// Icon colors
  static const Color iconPrimary = Color(0xFF000000);
  static const Color iconSecondary = Color(0xFF4A4A4A);
  static const Color iconDisabled = Color(0xFFBDBDBD);

  /// Divider color
  static const Color divider = Color(0xFFDDDDDD);

  /// Card color
  static const Color card = Colors.white;

  /// Shadow color
  static const Color shadow = Color(0x1A000000);
}
